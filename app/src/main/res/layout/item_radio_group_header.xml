<?xml version="1.0" encoding="utf-8"?>
<!--
  Radio category header.  Background is set programmatically by RadioGroupAdapter
  so it can respond to expanded/collapsed state with correct corner radii.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="52dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="14dp"
    android:paddingEnd="10dp"
    android:layout_marginTop="6dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Category name -->
    <TextView
        android:id="@+id/tvGroupLabel"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="#8888AA"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="0.02"
        android:maxLines="1"
        android:ellipsize="end"/>

    <!-- Station count badge -->
    <TextView
        android:id="@+id/tvGroupCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:fontFamily="sans-serif-medium"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="3dp"
        android:paddingBottom="3dp"
        android:layout_marginEnd="8dp"
        android:visibility="visible"/>

    <!-- Expand/collapse arrow -->
    <TextView
        android:id="@+id/tvGroupArrow"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:text="›"
        android:textSize="20sp"
        android:gravity="center"
        android:textColor="#44445A"/>

</LinearLayout>