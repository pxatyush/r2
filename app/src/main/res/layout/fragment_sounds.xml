<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark">

    <!-- GIF Visualizer (same as radio) -->
    <FrameLayout
        android:id="@+id/gif_container"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/bg_gif_container"
        android:margin="12dp">

        <pl.droidsonroids.gif.GifImageView
            android:id="@+id/gif_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:background="@color/bg_card"/>

        <!-- Stop All / Resume bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="#CC0D0D14"
            android:paddingHorizontal="16dp"
            android:paddingVertical="10dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tv_active_sounds"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="No sounds playing"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"/>

            <Button
                android:id="@+id/btn_stop_all"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="Stop All"
                android:textSize="11sp"
                android:textColor="@color/text_primary"
                android:backgroundTint="#2A2A4A"
                android:paddingHorizontal="14dp"/>
        </LinearLayout>
    </FrameLayout>

    <!-- Sounds grid -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_sounds"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="12dp"
        android:paddingBottom="12dp"
        android:clipToPadding="false"/>

</LinearLayout>
